# Push to GitHub

1. Create a **new empty repository** on GitHub named `code-review-agent`:
   - Go to https://github.com/new
   - Repository name: **code-review-agent**
   - Do **not** add a README, .gitignore, or license (this project already has them)

2. Push from Terminal (same machine where you pushed nas-file-processor):
   ```bash
   cd /Users/YeshiwSabore/Downloads/code-review-agent
   git push -u origin master
   ```

Remote is already set to: `https://github.com/ySabore/code-review-agent.git`
